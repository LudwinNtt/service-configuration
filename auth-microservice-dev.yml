bankapp:
  property: auth service profile dev
spring:
  data:
    mongodb:
      uri: mongodb://ludwin:.@localhost:27017/authentication?authSource=admin
      autoIndexCreation: true

jwt:
  secret: mykeysecret142as*1254$%&2121200.4545

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8084

